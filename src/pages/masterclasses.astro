---
import BaseLayout from '@layouts/BaseLayout.astro';
import MasterclassCard from '@components/MasterclassCard.astro';
import MasterclassesBanner from '@components/MasterclassesBanner.astro';
import FadeIn from '@components/FadeIn';
import { masterclasses } from '@data/masterclasses';
---

<BaseLayout 
  title="Masterclasses" 
  description="Join our professional beauty masterclasses and learn advanced techniques in skincare, brows, and SPMU."
>
  <MasterclassesBanner />

  <div class="border-t border-nude-beige/30"></div>

  <section class="py-20 bg-white">
    <div class="container mx-auto px-4">
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
        {masterclasses.map((masterclass, index) => (
          <FadeIn client:load delay={index * 0.1}>
            <MasterclassCard masterclass={masterclass} />
          </FadeIn>
        ))}
      </div>

      <div class="mt-16 mb-16 border-t border-nude-beige/30"></div>

      <FadeIn client:load delay={0.4}>
        <section class="py-12 bg-gradient-to-r from-gold/10 to-nude-beige/20">
          <div class="container mx-auto px-4">
            <div class="text-center">
          <h2 class="text-2xl font-heading font-semibold text-dark-brown mb-4">
            Have Questions About Our Masterclasses?
          </h2>
          <p class="text-dark-brown/70 mb-6">
            Contact us to learn more about our training programs.
          </p>
          <a 
            href="/contact" 
            class="inline-block bg-blush-pink text-white px-8 py-4 rounded-full text-lg font-medium hover:bg-blush-pink/90 transition-colors"
          >
            Contact Us
          </a>
            </div>
          </div>
        </section>
      </FadeIn>
    </div>
  </section>
</BaseLayout>

